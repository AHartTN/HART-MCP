version: '3.8'

services:
  hart-mcp:
    volumes:
      - .:/app:rw
    environment:
      - PYTHONUNBUFFERED=1
    command: ["python", "-m", "uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8000", "--reload", "--log-level", "info"]
